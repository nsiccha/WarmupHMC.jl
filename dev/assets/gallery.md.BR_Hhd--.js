import{_ as r,o as i,c as n,an as a,j as e,a as o}from"./chunks/framework.I2fQe_Lr.js";const u=JSON.parse('{"title":"Gallery","description":"","frontmatter":{},"headers":[],"relativePath":"gallery.md","filePath":"gallery.md","lastUpdated":null}'),d={name:"gallery.md"};function s(l,t,h,c,p,g){return i(),n("div",null,[...t[0]||(t[0]=[a('<h1 id="Gallery" tabindex="-1">Gallery <a class="header-anchor" href="#Gallery" aria-label="Permalink to &quot;Gallery {#Gallery}&quot;">​</a></h1><p>The WarmupHMC dashboard runs every PosteriorDB model through four samplers (WarmupHMC, DynamicHMC, AdvancedHMC, plus an optional reparametrised pass) and reports compile / sample / ESS / time per posterior. The views below are the live dashboard during development; in the deployed docs they are a <strong>static recording made during the docs build</strong>. The recording is never committed — <code>*.html</code> is ignored, so nothing under <code>docs/src/public/live-whmc/</code> is tracked, and CI regenerates it on every build.</p><h2 id="Runnable-examples" tabindex="-1">Runnable examples <a class="header-anchor" href="#Runnable-examples" aria-label="Permalink to &quot;Runnable examples {#Runnable-examples}&quot;">​</a></h2><p>The dashboard below is a recording of runs, not something you can step through. The examples that you <em>can</em> run are, smallest first:</p><table tabindex="0"><thead><tr><th style="text-align:right;">Example</th><th style="text-align:right;">Where</th><th style="text-align:right;">Executed at build time?</th></tr></thead><tbody><tr><td style="text-align:right;">Sample a 3-D Gaussian, read the draws back</td><td style="text-align:right;"><a href="/WarmupHMC.jl/dev/index#Quickstart">Quickstart</a></td><td style="text-align:right;"><strong>yes</strong></td></tr><tr><td style="text-align:right;">Checkpoint a run and resume it</td><td style="text-align:right;"><a href="/WarmupHMC.jl/dev/index#Checkpoints,-Callbacks-and-Resume">Checkpoints, Callbacks and Resume</a></td><td style="text-align:right;"><strong>yes</strong></td></tr><tr><td style="text-align:right;">Fit a nonlinear reparametrization on Neal&#39;s funnel</td><td style="text-align:right;"><a href="/WarmupHMC.jl/dev/reparametrization#A-complete-worked-example">A complete worked example</a></td><td style="text-align:right;">no — needs an AD backend the docs environment does not carry</td></tr><tr><td style="text-align:right;">Adapt a BRM scalar random-intercept model</td><td style="text-align:right;"><a href="/WarmupHMC.jl/dev/adaptive-centering#A-BRM-random-intercept-example">A BRM random-intercept example</a></td><td style="text-align:right;">no — needs BRM, BridgeStan and Enzyme</td></tr></tbody></table><p>&quot;Executed at build time&quot; means the outputs on those pages are what the code returned during this build, and that a change breaking the example fails the build rather than leaving a page that reads correctly and is wrong. A <code>no</code> does not mean incomplete — those examples are self-contained, they just need something the docs environment deliberately does not carry, so nothing checks them. See <a href="/WarmupHMC.jl/dev/index#A-note-on-the-code-blocks">A note on the code blocks</a>.</p><p><strong>Adding a runnable example? Add its row here.</strong> This table is the only place the executed/not-executed split is written down; the prose elsewhere points at it rather than repeating it.</p><h2 id="Overview-table" tabindex="-1">Overview table <a class="header-anchor" href="#Overview-table" aria-label="Permalink to &quot;Overview table {#Overview-table}&quot;">​</a></h2>',8),e("div",{class:"htmxo-embed-fullwidth"},[e("div",{class:"htmxo-embed","data-hx-base":"live-whmc/","hx-trigger":"load","hx-swap":"innerHTML"},[e("em",null,"Loading WHMC overview…")])],-1),e("h2",{id:"Card-grid",tabindex:"-1"},[o("Card grid "),e("a",{class:"header-anchor",href:"#Card-grid","aria-label":'Permalink to "Card grid {#Card-grid}"'},"​")],-1),e("div",{class:"htmxo-embed-fullwidth"},[e("div",{class:"htmxo-embed","data-hx-base":"live-whmc/gallery","hx-trigger":"load","hx-swap":"innerHTML"},[e("em",null,"Loading WHMC gallery…")])],-1),a('<h2 id="What-the-recording-does-and-does-not-include" tabindex="-1">What the recording does and does not include <a class="header-anchor" href="#What-the-recording-does-and-does-not-include" aria-label="Permalink to &quot;What the recording does and does not include {#What-the-recording-does-and-does-not-include}&quot;">​</a></h2><p>The recorder writes full-page and HX-shape variants of exactly two routes — <code>/</code> and <code>/gallery</code> — into <code>docs/src/public/live-whmc/</code>. That is the whole export.</p><p>The live app serves more than that: per-posterior routes under <code>/posteriors/&lt;name&gt;</code>, plus <code>/benchmarks</code> and <code>/benchmark/&lt;key&gt;</code>. <strong>None of those are recorded</strong>, and the export deliberately removes the links that would lead to them, so the two views above are complete rather than a surface with dead ends behind it. Recording is done in a static mode that also turns the per-posterior detail panels into inert placeholders and disables the compute controls; the overview and card surfaces are intact, but nothing in a recording computes.</p><p>The static benchmark evidence is not here at all — it is native Markdown on <a href="/WarmupHMC.jl/dev/evidence#Benchmark-evidence">Benchmark evidence</a>, generated from the same JSON.</p><h2 id="Refresh-the-recording" tabindex="-1">Refresh the recording <a class="header-anchor" href="#Refresh-the-recording" aria-label="Permalink to &quot;Refresh the recording {#Refresh-the-recording}&quot;">​</a></h2><p>CI runs the recorder before building the docs, so a deployed page is always a recording of the revision it was built from. To reproduce it locally, after instantiating the web environment:</p><div class="language-sh vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">julia</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --project=web</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> docs/record_gallery.jl</span></span></code></pre></div><p>That is the supported path. It records the two docs routes, writes both shapes, and fails loudly if any expected file is missing.</p><div class="warning custom-block"><p class="custom-block-title"><code>GET /record_gallery</code> is not the docs recipe</p><p>The running app still exposes that route, but without the static-recording mode the script sets it uses the app&#39;s <strong>full</strong> path list — including the per-posterior routes, which are expensive. Use the script above for anything aimed at the docs.</p></div>',9)])])}const b=r(d,[["render",s]]);export{u as __pageData,b as default};
